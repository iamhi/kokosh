Do three things using the file tools:
1. Use glob_files to find all .js files under src/handlers/llmhandler/permissions/
2. Use read_file to read src/handlers/llmhandler/permissions/directoryGuard.js
3. Use grep_files to search for the word "checkDirectoryAccess" across the whole project

All three paths are inside C:/Projects/kokosh, so access should be granted. Confirm that each tool call succeeded and summarise what you found.
